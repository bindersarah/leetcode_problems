class Solution:
    def buildArray(self, nums: List[int]) -> List[int]:
        n = len(nums)
        output = []
        for i in range(n):
            a = nums[nums[i]]
            output.append(a)
        return output
